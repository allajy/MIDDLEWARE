spring:
  cloud:
    nacos:
      config:
        server-addr: ${NACOS_ADDRESS:101.37.27.179}:8848
      discovery:
        server-addr: ${NACOS_ADDRESS:101.37.27.179}:8848
    gateway:
      discovery:
        locator:
          # gateway通过服务发现组件找到其他的微服务，从而自动转发请求
          enabled: true
